extends ../layout
block vars
    - var userId = userId 
    - var isUser = isUser 
    - var isOwner = isOwner

block content
    h1.congratulations-title CONGRATULATIONS!
    if error 
        p(class="error", style="color:red;")= error 
    p.congratulations-p You have created a new project!