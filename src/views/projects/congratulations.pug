extends ../layout
block vars
    - var userId = userId 
    - var isUser = isUser 
    - var isOwner = isOwner

block content
    h1 Congratulations
    if error 
        p(class="error", style="color:red;")= error 